# ============================================================================
# DentFlow Backend - Environment Configuration Example
# ============================================================================
# Copy this file to .env and fill in the actual values for your environment
# This file is for reference only and should NOT be committed with real values
# ============================================================================

# Django Settings
# ============================================================================
# SECRET_KEY: Django secret key for cryptographic signing
# IMPORTANT: Use a strong, random value in production - never commit the real key
# Generate one with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
#SECRET_KEY=your-secret-key-here

# DEBUG: Set to False in production for security
# Only set to True during development
DEBUG=True

# ALLOWED_HOSTS: Comma-separated list of allowed domain/IP addresses
# Examples: localhost,127.0.0.1,example.com,api.example.com
ALLOWED_HOSTS=localhost,127.0.0.1

# ============================================================================
# Supabase Configuration (REQUIRED)
# ============================================================================
# Get these values from your Supabase project dashboard:
# https://app.supabase.com/

# SUPABASE_URL: Your Supabase project URL
# Format: https://xxxxx.supabase.co
SUPABASE_URL=https://your-project-id.supabase.co

# SUPABASE_KEY: Your Supabase API key (anon or service_role)
# WARNING: Keep this secret in production, never commit the real key
SUPABASE_KEY=your-supabase-api-key-here

# SUPABASE_CONNECTION_TIMEOUT: Connection timeout in seconds (default: 30)
SUPABASE_CONNECTION_TIMEOUT=30

# SUPABASE_MAX_RETRIES: Maximum retry attempts for transient failures (default: 3)
SUPABASE_MAX_RETRIES=3

# ============================================================================
# CORS Configuration
# ============================================================================
# CORS_ALLOWED_ORIGINS: Comma-separated list of allowed frontend origins
# Examples: http://localhost:3000,https://example.com
# Leave empty to use CORS_ALLOW_ALL_ORIGINS=True setting (development only)
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# ============================================================================
# Logging Configuration
# ============================================================================
# Log levels: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Logging is configured in settings.py
# In production (DEBUG=False), logs are written to logs/ directory with rotation
